import React from "react";

type Props = {
    filter:any,
    onChange:any,
    defaultValue?:any,
}

const FormulaFilter = ({filter, onChange, defaultValue}:Props) => {
    return <></>
};

export {FormulaFilter};
