import React from "react";

type Props = {
    filter:any,
    onChange:any,
    defaultValue?:any,
}

const DropdownRangeFilter = ({filter, onChange, defaultValue}:Props) => {
    return <></>
};

export {DropdownRangeFilter};
