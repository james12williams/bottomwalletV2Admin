import React from "react";

type Props = {
    filter:any,
    onChange:any,
    defaultValue?:any,
}

const DropdownMultipleRangeFilter = ({filter, onChange, defaultValue}:Props) => {
    return <></>
};

export {DropdownMultipleRangeFilter};
